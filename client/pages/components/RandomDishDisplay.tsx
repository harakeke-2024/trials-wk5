import { DishWithFilm } from '../../../models/ghibli'

interface Props {
  dish: DishWithFilm
}

export default function RandomDishDisplay({ dish }: Props) {
  console.log(obj)
  return <p>hello</p>
}
